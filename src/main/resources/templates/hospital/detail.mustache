{{>layouts/header}}


<div class="container">
    <h1>detail page</h1>

    <table class="table">
        <thead>
        <tr>
            <th scope="col">No</th>
            <th scope="col">병원명</th>
            <th scope="col">전화번호</th>
            <th scope="col">위치</th>
        </tr>
        </thead>
        {{#hospital}}
            <tbody>
            <tr>
                <td>{{id}}</td>
                <td>{{hospitalName}}</td>
                <td>{{phone}}</td>
                <td>{{fullAddress}}</td>
            </tr>
            </tbody>
            </tr>
            </tbody>
        {{/hospital}}
    </table>
</div>

<div class="container">
    <a href="/hospital/list">
        <button type="button" class="btn btn-success">Go List</button>
    </a>
</div>
{{>layouts/footer}}